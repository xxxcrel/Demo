spring:
  data:
    jpa:
      repositories:
        enabled: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    username: root
    password: root
    url: jdbc:h2:~/repository_test
  test:
    database:
      replace: auto_configured
logging:
  level:
    root: off