spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    username: root
    password: xc514xxx
    driver-class-name:
    url: jdbc:mysql://localhost:3306/code_demo?serverTimezone=Asia/Shanghai
